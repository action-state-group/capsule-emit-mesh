# pip install -r requirements.txt
#
# capsule-emit[mcp] pulls agent-action-capsule and capsule-emit.
# scitt-cose and cbor2 are NOT pulled by capsule-emit[mcp]; they must be declared
# here because capsule_sidecar.py imports scitt_cose directly.
# mcp>=2.0 removed mcp.server.fastmcp; the <2 pin keeps goose/server.py importable
# until it migrates to the standalone fastmcp package.
#
# capsule_emit.checkpoint (Layers 1-2) and scitt_cose.cll shipped in released
# versions (capsule-emit 0.4.0, scitt-cose 0.2.2); the prior git-ref pins here
# (capsule-emit PR #66, scitt-cose PR #38/#40) are no longer needed.
#
# Floor is 0.5.1 (the current release), not 0.4.0: capsule-emit 0.5.1 pulls
# agent-action-capsule 0.2.0, whose format-4/JCS capsules carry different
# capsule_id/digest bytes than 0.4.x and whose top-level emit() verb was
# renamed to seal()/received(). This repo consumes only the stable
# capsule_emit.checkpoint surface and agent_action_capsule.emit() (aac's own
# emit, unchanged in 0.2.0) -- not capsule-emit's removed emit() verb -- so the
# full suite passes and the committed ledger/ fixtures verify clean against
# 0.5.1/0.2.0 with no migration. The floor is pinned at the tested release to
# prevent silent resolution back to an older, format-incompatible line.
capsule-emit[mcp]>=0.5.1
scitt-cose>=0.2.2
cbor2
mcp<2
# nostr_account.py signs account-capsule events with BIP-340 Schnorr over
# secp256k1 (NIP-01) -- the curve Nostr uses, which the stdlib and `cryptography`
# do not expose. coincurve provides sign_schnorr / PublicKeyXOnly. The account
# capsule build path and the mock-relay publish path load without it; only the
# real Schnorr signing key (SchnorrNostrKey) needs it, and the tests skip if
# it is absent.
coincurve>=18
