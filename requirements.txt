# pip install -r requirements.txt
#
# capsule-emit[mcp] pulls agent-action-capsule and capsule-emit.
# scitt-cose and cbor2 are NOT pulled by capsule-emit[mcp]; they must be declared
# here because capsule_sidecar.py imports scitt_cose directly.
# mcp>=2.0 removed mcp.server.fastmcp; the <2 pin keeps goose/server.py importable
# until it migrates to the standalone fastmcp package.
#
# TEMPORARY git-ref pins (checkpointing.py / [checkpoint] config, Layers 1-2):
# capsule_emit.checkpoint and scitt_cose.cll are MERGED to both projects'
# main (capsule-emit PR #66, scitt-cose PR #38 -- updated 2026-08-22, was
# pinned to the pre-merge feature-branch commits) but NEITHER has cut a
# release with these modules yet (both still report their pre-checkpoint
# pyproject version). Pinned to each project's current main commit rather
# than a feature branch now that both have landed. SWAP THESE TWO LINES to
# plain `capsule-emit[mcp]>=0.5.0` / `scitt-cose>=0.2.0` (or whatever
# versions ship the release) once a version bump ships -- do not leave a
# git-ref pin in main past that point.
capsule-emit[mcp] @ git+https://github.com/action-state-group/capsule-emit.git@514e0d32ba8a2ee564a8c6de992c1cb64885c3ae
scitt-cose @ git+https://github.com/action-state-group/scitt-cose.git@056e5e1c6514d611c60bd6abb131db4c7167c156
cbor2
mcp<2
