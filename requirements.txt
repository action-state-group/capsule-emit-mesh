# pip install -r requirements.txt
#
# capsule-emit[mcp] pulls agent-action-capsule and capsule-emit.
# scitt-cose and cbor2 are NOT pulled by capsule-emit[mcp]; they must be declared
# here because capsule_sidecar.py imports scitt_cose directly.
# mcp>=2.0 removed mcp.server.fastmcp; the <2 pin keeps goose/server.py importable
# until it migrates to the standalone fastmcp package.
#
# capsule_emit.checkpoint (Layers 1-2) and scitt_cose.cll shipped in released
# versions (capsule-emit 0.4.0, scitt-cose 0.2.2); the prior git-ref pins here
# (capsule-emit PR #66, scitt-cose PR #38/#40) are no longer needed.
#
# Floor was 0.5.1, not 0.4.0: capsule-emit 0.5.1 pulls agent-action-capsule
# 0.2.0, whose format-4/JCS capsules carry different capsule_id/digest bytes
# than 0.4.x and whose top-level emit() verb was renamed to seal()/received().
# This repo consumes only the stable capsule_emit.checkpoint surface and
# agent_action_capsule.emit() (aac's own emit, unchanged in 0.2.0) -- not
# capsule-emit's removed emit() verb -- so the full suite passes and the
# committed ledger/ fixtures verify clean with no migration.
#
# Floor was 0.6.1: capsule-emit#128/#135 fixed a bug where a float-typed tool
# argument dropped the planned capsule at the digest layer instead of chaining
# it. No fixtures here carry float tool-args, so no regeneration was needed
# for that bump.
#
# Floor is now 0.7.0 (the current release): the CLL/MMR core (checkpoint
# logging + Merkle Mountain Range) graduated out of capsule-emit into its own
# package, checkpointed-local-log>=0.1.0, which capsule-emit[mcp] now pulls
# transitively as a core (non-extra) dependency -- no separate pin needed
# here. 0.7.0 also folds in the 0.6.1 float tool-arg canonicalization fix.
# The floor is pinned at the tested release to prevent silent resolution back
# to an older line.
capsule-emit[mcp]>=0.7.0
scitt-cose>=0.2.2
cbor2
mcp<2

# tomllib is stdlib from Python 3.11; checkpointing.py's load_checkpoint_config
# falls back to the tomli backport so [checkpoint] TOML configs (checkpoint.example.toml)
# still parse on Python 3.10. Marker keeps this a no-op install on 3.11+.
tomli>=2.0; python_version < "3.11"

# trace_citation.py depends on agent-manifest's TDX quote verifier
# (agent_manifest._tdx_verify: parse_tdx_quote / verify_tdx_quote / TdxVerificationError) to grade a
# cited TRACE Trust Record's hardware evidence. This is a hard dependency, not vendored: the standing
# rule for this rung is "depend on their verifier, never re-implement" (mesh-tee-rung-trace-citation).
# TDX verification landed at 0.5.0 and a buffer-overrun parsing fix (#250) landed by 0.8.0; 0.11.2
# is the release we tested against, so the floor is pinned there rather than at the bare minimum.
agent-manifest>=0.11.2

# trace_citation.py's OPTIONAL record-authenticity check (only exercised when a caller passes a
# trusted issuer key -- no deployment does yet, pending a registry decision explicitly out of scope
# before 2026-09-09) depends on agentrust_trace.sign.verify_record, same never-re-implement rule.
# Pinned so the reject-on-tampered-envelope mutant test always runs in CI rather than skipping.
agentrust-trace>=0.9.0
